[options]
install_requires =
    # Include your other dependencies here if needed, 
    # but the main goal is to ignore the massive GPU one.

[options.package_data]
* = *.txt, *.rst

# THIS IS THE CRUCIAL PART: Ignore the massive GPU dependency
[options.extras_require]
gpu =